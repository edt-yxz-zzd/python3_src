

from distutils.core import setup

##import sys
##print(sys.path)

setup(name='show_pic',
      version='20150812',
      #packages=['sand'],
      package_dir={'':'src'},
      packages=[''],
      requires = ['PIL'],
      )
