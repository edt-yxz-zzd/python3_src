#g2
	echo ======= cp \*.sh
	export cmd='cp -r -u -i -t $my_git_sh/ $my_sh/*'
	echo ======= $cmd
	echo $cmd | bash
	#$cmd
	#exec $cmd
	#echo $cmd | bash -i -s -m -I -b
	#bash -c $cmd
	#env bash -i -c $cmd
