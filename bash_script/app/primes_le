

#e ../../python3_src/bash_script/app/primes_le
if [[ $# -ne 1 ]] ; then
  echo >&2 'USAGE: primes_le <MAX_PRIME>'
  exit 1
fi

py_adhoc_call   seed.math.prime_gens   ,raw_iter_all_strict_sorted_primes__lt_  -to_cache_only_busy_primes_plus_next --may_primes=None  ="1+(${1})"

